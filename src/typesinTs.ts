let drink = "water";

let cup = Math.random() > 0.5 ? 10 : 5;

let isHot: boolean = true;

let temperature: number = 75;

let message: string = `The drink is ${drink} and the cup size is ${cup} ounces.`;
console.log(message);
